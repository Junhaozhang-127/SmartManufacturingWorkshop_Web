generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model SysUser {
  id           BigInt          @id @default(autoincrement()) @db.UnsignedBigInt
  username     String          @unique @db.VarChar(64)
  passwordHash String          @map("password_hash") @db.VarChar(255)
  displayName  String          @map("display_name") @db.VarChar(64)
  mobile       String?         @db.VarChar(20)
  email        String?         @db.VarChar(128)
  statusCode   String          @default("ACTIVE") @map("status_code") @db.VarChar(20)
  forcePasswordChange Boolean  @default(true) @map("force_password_change")
  lastLoginAt  DateTime?       @map("last_login_at")
  passwordChangedAt DateTime?  @map("password_changed_at")
  createdAt    DateTime        @default(now()) @map("created_at")
  updatedAt    DateTime        @updatedAt @map("updated_at")
  createdBy    BigInt?         @map("created_by") @db.UnsignedBigInt
  isDeleted    Boolean         @default(false) @map("is_deleted")
  userRoles    SysUserRole[]
  member       MemberProfile?
  ledOrgUnits  OrgUnit[]       @relation("OrgUnitLeader")
  mentorFor    MemberProfile[] @relation("MemberMentor")
  memberGrowthRecords      MemberGrowthRecord[]      @relation("MemberGrowthActor")
  memberStageEvaluations   MemberStageEvaluation[]   @relation("MemberStageEvaluator")
  memberRegularizations    MemberRegularization[]    @relation("MemberRegularizationApplicant")
  memberOperationLogs      MemberOperationLog[]      @relation("MemberOperationActor")
  evaluationScoreRecords   EvalScoreRecord[]         @relation("EvaluationEvaluator")
  promotionApplications    PromApplication[]         @relation("PromotionApplicant")
  promotionAppointments    PromAppointment[]         @relation("PromotionAppointmentApplicant")
  rewardPenaltyRecords     GovRewardPenalty[]        @relation("RewardPenaltySubject")
  approvalInstancesStarted  WfApprovalInstance[] @relation("ApprovalApplicant")
  approvalInstancesAssigned WfApprovalInstance[] @relation("ApprovalCurrentAssignee")
  approvalLogs              WfApprovalNodeLog[]  @relation("ApprovalActor")
  approvalTransferTargets   WfApprovalNodeLog[]  @relation("ApprovalTransferTarget")
  demoApprovalForms         DemoApprovalForm[]   @relation("DemoApprovalApplicant")
  ledCompetitionTeams       CompTeam[]           @relation("CompetitionTeamLeader")
  advisedCompetitionTeams   CompTeam[]           @relation("CompetitionAdvisor")
  achievementApplications   AchvAchievement[]    @relation("AchievementApplicant")
  achievementContributions  AchvContributor[]    @relation("AchievementContributorUser")
  responsibleDevices        AssetDevice[]        @relation("DeviceResponsibleUser")
  deviceRepairApplicants    AssetDeviceRepair[]  @relation("DeviceRepairApplicant")
  deviceRepairHandlers      AssetDeviceRepair[]  @relation("DeviceRepairHandler")
  managedConsumables        InvConsumable[]      @relation("ConsumableCreatedByUser")
  inventoryTransactions     InvInventoryTxn[]    @relation("InventoryTxnOperator")
  consumableRequests        InvConsumableRequest[] @relation("ConsumableRequestApplicant")
  managedFundAccounts       FundAccount[]        @relation("FundAccountManager")
  fundApplications          FundApplication[]    @relation("FundApplicationApplicant")
  notifications             SysNotification[]

  @@map("sys_user")
}

model SysRole {
  id         BigInt        @id @default(autoincrement()) @db.UnsignedBigInt
  roleCode   String        @unique @map("role_code") @db.VarChar(32)
  roleName   String        @map("role_name") @db.VarChar(64)
  dataScope  String        @map("data_scope") @db.VarChar(20)
  sortNo     Int?          @map("sort_no")
  statusCode String        @default("ACTIVE") @map("status_code") @db.VarChar(20)
  createdAt  DateTime      @default(now()) @map("created_at")
  updatedAt  DateTime      @updatedAt @map("updated_at")
  userRoles  SysUserRole[]

  @@map("sys_role")
}

model SysUserRole {
  id            BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  userId        BigInt    @map("user_id") @db.UnsignedBigInt
  roleId        BigInt    @map("role_id") @db.UnsignedBigInt
  effectiveFrom DateTime? @map("effective_from")
  effectiveTo   DateTime? @map("effective_to")
  createdAt     DateTime  @default(now()) @map("created_at")
  updatedAt     DateTime  @updatedAt @map("updated_at")
  role          SysRole   @relation(fields: [roleId], references: [id])
  user          SysUser   @relation(fields: [userId], references: [id])

  @@unique([userId, roleId])
  @@map("sys_user_role")
}

model SysNotification {
  id           BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  userId       BigInt    @map("user_id") @db.UnsignedBigInt
  title        String    @db.VarChar(128)
  content      String    @db.VarChar(1000)
  categoryCode String    @default("GENERAL") @map("category_code") @db.VarChar(32)
  levelCode    String    @default("INFO") @map("level_code") @db.VarChar(16)
  businessType String?   @map("business_type") @db.VarChar(64)
  businessId   String?   @map("business_id") @db.VarChar(64)
  routePath    String?   @map("route_path") @db.VarChar(255)
  routeQuery   Json?     @map("route_query")
  readAt       DateTime? @map("read_at")
  createdAt    DateTime  @default(now()) @map("created_at")
  updatedAt    DateTime  @updatedAt @map("updated_at")
  createdBy    BigInt?   @map("created_by") @db.UnsignedBigInt
  isDeleted    Boolean   @default(false) @map("is_deleted")
  user         SysUser   @relation(fields: [userId], references: [id])

  @@index([userId, readAt, createdAt])
  @@index([businessType, businessId])
  @@map("sys_notification")
}

model SysDictType {
  id          BigInt        @id @default(autoincrement()) @db.UnsignedBigInt
  dictCode    String        @unique @map("dict_code") @db.VarChar(64)
  dictName    String        @map("dict_name") @db.VarChar(128)
  description String?       @db.VarChar(255)
  statusCode  String        @default("ACTIVE") @map("status_code") @db.VarChar(20)
  systemFlag  Boolean       @default(false) @map("system_flag")
  createdAt   DateTime      @default(now()) @map("created_at")
  updatedAt   DateTime      @updatedAt @map("updated_at")
  items       SysDictItem[]

  @@map("sys_dict_type")
}

model SysDictItem {
  id         BigInt      @id @default(autoincrement()) @db.UnsignedBigInt
  dictTypeId BigInt      @map("dict_type_id") @db.UnsignedBigInt
  itemCode   String      @map("item_code") @db.VarChar(64)
  itemLabel  String      @map("item_label") @db.VarChar(128)
  itemValue  String      @map("item_value") @db.VarChar(128)
  sortNo     Int         @default(0) @map("sort_no")
  statusCode String      @default("ACTIVE") @map("status_code") @db.VarChar(20)
  extData    Json?       @map("ext_data")
  createdAt  DateTime    @default(now()) @map("created_at")
  updatedAt  DateTime    @updatedAt @map("updated_at")
  dictType   SysDictType @relation(fields: [dictTypeId], references: [id])

  @@unique([dictTypeId, itemCode])
  @@unique([dictTypeId, itemValue])
  @@index([dictTypeId, sortNo])
  @@map("sys_dict_item")
}

model SysConfigItem {
  id             BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  configCategory String   @map("config_category") @db.VarChar(64)
  configKey      String   @unique @map("config_key") @db.VarChar(64)
  configName     String   @map("config_name") @db.VarChar(128)
  configValue    String   @map("config_value") @db.Text
  valueType      String   @default("TEXT") @map("value_type") @db.VarChar(32)
  statusCode     String   @default("ACTIVE") @map("status_code") @db.VarChar(20)
  remark         String?  @db.VarChar(500)
  editable       Boolean  @default(true)
  createdAt      DateTime @default(now()) @map("created_at")
  updatedAt      DateTime @updatedAt @map("updated_at")

  @@index([configCategory, statusCode])
  @@map("sys_config_item")
}

model OrgUnit {
  id             BigInt          @id @default(autoincrement()) @db.UnsignedBigInt
  parentId       BigInt?         @map("parent_id") @db.UnsignedBigInt
  unitCode       String          @unique @map("unit_code") @db.VarChar(32)
  unitName       String          @map("unit_name") @db.VarChar(128)
  unitType       String          @map("unit_type") @db.VarChar(32)
  leaderUserId   BigInt?         @map("leader_user_id") @db.UnsignedBigInt
  statusCode     String          @default("ACTIVE") @map("status_code") @db.VarChar(20)
  createdAt      DateTime        @default(now()) @map("created_at")
  updatedAt      DateTime        @updatedAt @map("updated_at")
  createdBy      BigInt?         @map("created_by") @db.UnsignedBigInt
  isDeleted      Boolean         @default(false) @map("is_deleted")
  children       OrgUnit[]       @relation("OrgUnitTree")
  leader         SysUser?        @relation("OrgUnitLeader", fields: [leaderUserId], references: [id])
  memberProfiles MemberProfile[]
  assetDevices   AssetDevice[]
  consumables    InvConsumable[]
  fundAccounts   FundAccount[]
  parent         OrgUnit?        @relation("OrgUnitTree", fields: [parentId], references: [id])

  @@index([parentId])
  @@map("org_unit")
}

model MemberProfile {
  id           BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  userId       BigInt   @unique @map("user_id") @db.UnsignedBigInt
  orgUnitId    BigInt   @map("org_unit_id") @db.UnsignedBigInt
  positionCode String   @map("position_code") @db.VarChar(32)
  mentorUserId BigInt?  @map("mentor_user_id") @db.UnsignedBigInt
  joinDate     DateTime @map("join_date") @db.Date
  memberStatus String   @map("member_status") @db.VarChar(20)
  skillTags    String?  @map("skill_tags") @db.VarChar(255)
  createdAt    DateTime @default(now()) @map("created_at")
  updatedAt    DateTime @updatedAt @map("updated_at")
  createdBy    BigInt?  @map("created_by") @db.UnsignedBigInt
  isDeleted    Boolean  @default(false) @map("is_deleted")
  mentor       SysUser? @relation("MemberMentor", fields: [mentorUserId], references: [id])
  orgUnit      OrgUnit  @relation(fields: [orgUnitId], references: [id])
  user         SysUser  @relation(fields: [userId], references: [id])
  growthRecords MemberGrowthRecord[]
  stageEvaluations MemberStageEvaluation[]
  regularizations MemberRegularization[]
  operationLogs MemberOperationLog[]
  evaluationScores EvalScoreRecord[]
  promotionApplications PromApplication[]
  promotionAppointments PromAppointment[]
  rewardPenaltyRecords GovRewardPenalty[]

  @@index([orgUnitId])
  @@map("member_profile")
}

model MemberGrowthRecord {
  id              BigInt        @id @default(autoincrement()) @db.UnsignedBigInt
  memberProfileId BigInt        @map("member_profile_id") @db.UnsignedBigInt
  recordType      String        @map("record_type") @db.VarChar(32)
  title           String        @db.VarChar(128)
  content         String?       @db.VarChar(1000)
  recordDate      DateTime      @map("record_date") @db.Date
  actorUserId     BigInt?       @map("actor_user_id") @db.UnsignedBigInt
  extraData       Json?         @map("extra_data")
  createdAt       DateTime      @default(now()) @map("created_at")
  updatedAt       DateTime      @updatedAt @map("updated_at")
  actor           SysUser?      @relation("MemberGrowthActor", fields: [actorUserId], references: [id])
  memberProfile   MemberProfile @relation(fields: [memberProfileId], references: [id])

  @@index([memberProfileId, recordDate])
  @@map("member_growth_record")
}

model MemberStageEvaluation {
  id                BigInt        @id @default(autoincrement()) @db.UnsignedBigInt
  memberProfileId   BigInt        @map("member_profile_id") @db.UnsignedBigInt
  stageCode         String        @map("stage_code") @db.VarChar(32)
  summary           String        @db.VarChar(500)
  score             Int?
  resultCode        String        @map("result_code") @db.VarChar(32)
  nextAction        String?       @map("next_action") @db.VarChar(500)
  evaluatedAt       DateTime      @map("evaluated_at")
  evaluatorUserId   BigInt        @map("evaluator_user_id") @db.UnsignedBigInt
  createdAt         DateTime      @default(now()) @map("created_at")
  updatedAt         DateTime      @updatedAt @map("updated_at")
  evaluator         SysUser       @relation("MemberStageEvaluator", fields: [evaluatorUserId], references: [id])
  memberProfile     MemberProfile @relation(fields: [memberProfileId], references: [id])

  @@index([memberProfileId, evaluatedAt])
  @@map("member_stage_evaluation")
}

model MemberRegularization {
  id                  BigInt              @id @default(autoincrement()) @db.UnsignedBigInt
  memberProfileId     BigInt              @map("member_profile_id") @db.UnsignedBigInt
  applicantUserId     BigInt              @map("applicant_user_id") @db.UnsignedBigInt
  statusCode          String              @default("DRAFT") @map("status_code") @db.VarChar(32)
  internshipStartDate DateTime            @map("internship_start_date") @db.Date
  plannedRegularDate  DateTime            @map("planned_regular_date") @db.Date
  applicationReason   String              @map("application_reason") @db.VarChar(1000)
  selfAssessment      String?             @map("self_assessment") @db.VarChar(1000)
  mentorUserId        BigInt?             @map("mentor_user_id") @db.UnsignedBigInt
  approvalInstanceId  BigInt?             @unique @map("approval_instance_id") @db.UnsignedBigInt
  submittedAt         DateTime?           @map("submitted_at")
  completedAt         DateTime?           @map("completed_at")
  latestResult        String?             @map("latest_result") @db.VarChar(500)
  createdAt           DateTime            @default(now()) @map("created_at")
  updatedAt           DateTime            @updatedAt @map("updated_at")
  applicant           SysUser             @relation("MemberRegularizationApplicant", fields: [applicantUserId], references: [id])
  memberProfile       MemberProfile       @relation(fields: [memberProfileId], references: [id])
  approvalInstance    WfApprovalInstance? @relation(fields: [approvalInstanceId], references: [id])

  @@index([memberProfileId, statusCode])
  @@index([applicantUserId, statusCode])
  @@map("member_regularization")
}

model MemberOperationLog {
  id              BigInt        @id @default(autoincrement()) @db.UnsignedBigInt
  memberProfileId BigInt        @map("member_profile_id") @db.UnsignedBigInt
  actionType      String        @map("action_type") @db.VarChar(32)
  fromStatus      String?       @map("from_status") @db.VarChar(32)
  toStatus        String?       @map("to_status") @db.VarChar(32)
  description     String        @db.VarChar(500)
  operatorUserId  BigInt?       @map("operator_user_id") @db.UnsignedBigInt
  extraData       Json?         @map("extra_data")
  createdAt       DateTime      @default(now()) @map("created_at")
  memberProfile   MemberProfile @relation(fields: [memberProfileId], references: [id])
  operator        SysUser?      @relation("MemberOperationActor", fields: [operatorUserId], references: [id])

  @@index([memberProfileId, createdAt])
  @@map("member_operation_log")
}

model EvalScheme {
  id                   BigInt            @id @default(autoincrement()) @db.UnsignedBigInt
  schemeCode           String            @unique @map("scheme_code") @db.VarChar(64)
  schemeName           String            @map("scheme_name") @db.VarChar(128)
  periodKey            String            @map("period_key") @db.VarChar(32)
  startDate            DateTime          @map("start_date") @db.Date
  endDate              DateTime          @map("end_date") @db.Date
  statusCode           String            @default("ACTIVE") @map("status_code") @db.VarChar(20)
  ruleConfig           Json?             @map("rule_config")
  createdAt            DateTime          @default(now()) @map("created_at")
  updatedAt            DateTime          @updatedAt @map("updated_at")
  scoreRecords         EvalScoreRecord[]
  promotionApplications PromApplication[]

  @@index([statusCode, startDate, endDate])
  @@map("eval_scheme")
}

model EvalScoreRecord {
  id                 BigInt        @id @default(autoincrement()) @db.UnsignedBigInt
  schemeId           BigInt        @map("scheme_id") @db.UnsignedBigInt
  memberProfileId    BigInt        @map("member_profile_id") @db.UnsignedBigInt
  evaluatorUserId    BigInt?       @map("evaluator_user_id") @db.UnsignedBigInt
  achievementCount   Int           @default(0) @map("achievement_count")
  projectCount       Int           @default(0) @map("project_count")
  rewardPenaltyCount Int           @default(0) @map("reward_penalty_count")
  autoScore          Decimal       @default(0) @map("auto_score") @db.Decimal(8, 2)
  manualScore        Decimal       @default(0) @map("manual_score") @db.Decimal(8, 2)
  totalScore         Decimal       @default(0) @map("total_score") @db.Decimal(8, 2)
  autoScoreDetail    Json?         @map("auto_score_detail")
  manualScoreDetail  Json?         @map("manual_score_detail")
  manualComment      String?       @map("manual_comment") @db.VarChar(500)
  resultCode         String        @map("result_code") @db.VarChar(32)
  latestResult       String?       @map("latest_result") @db.VarChar(500)
  evaluatedAt        DateTime?     @map("evaluated_at")
  createdAt          DateTime      @default(now()) @map("created_at")
  updatedAt          DateTime      @updatedAt @map("updated_at")
  evaluator          SysUser?      @relation("EvaluationEvaluator", fields: [evaluatorUserId], references: [id])
  memberProfile      MemberProfile @relation(fields: [memberProfileId], references: [id])
  scheme             EvalScheme    @relation(fields: [schemeId], references: [id])

  @@unique([schemeId, memberProfileId])
  @@index([memberProfileId, updatedAt])
  @@map("eval_score_record")
}

model GovRewardPenalty {
  id              BigInt        @id @default(autoincrement()) @db.UnsignedBigInt
  memberProfileId BigInt        @map("member_profile_id") @db.UnsignedBigInt
  subjectUserId   BigInt        @map("subject_user_id") @db.UnsignedBigInt
  eventType       String        @map("event_type") @db.VarChar(16)
  title           String        @db.VarChar(128)
  levelCode       String?       @map("level_code") @db.VarChar(32)
  scoreImpact     Decimal       @default(0) @map("score_impact") @db.Decimal(8, 2)
  sourceType      String?       @map("source_type") @db.VarChar(32)
  sourceRefId     String?       @map("source_ref_id") @db.VarChar(64)
  description     String?       @db.VarChar(500)
  occurredAt      DateTime      @map("occurred_at") @db.Date
  createdAt       DateTime      @default(now()) @map("created_at")
  updatedAt       DateTime      @updatedAt @map("updated_at")
  memberProfile   MemberProfile @relation(fields: [memberProfileId], references: [id])
  subjectUser     SysUser       @relation("RewardPenaltySubject", fields: [subjectUserId], references: [id])

  @@index([memberProfileId, occurredAt])
  @@index([subjectUserId, occurredAt])
  @@map("gov_reward_penalty")
}

model WfApprovalTemplate {
  id           BigInt                   @id @default(autoincrement()) @db.UnsignedBigInt
  templateCode String                   @unique @map("template_code") @db.VarChar(64)
  templateName String                   @map("template_name") @db.VarChar(128)
  businessType String                   @unique @map("business_type") @db.VarChar(64)
  statusCode   String                   @default("ACTIVE") @map("status_code") @db.VarChar(20)
  createdAt    DateTime                 @default(now()) @map("created_at")
  updatedAt    DateTime                 @updatedAt @map("updated_at")
  nodes        WfApprovalTemplateNode[]
  instances    WfApprovalInstance[]

  @@map("wf_approval_template")
}

model WfApprovalTemplateNode {
  id               BigInt             @id @default(autoincrement()) @db.UnsignedBigInt
  templateId       BigInt             @map("template_id") @db.UnsignedBigInt
  nodeKey          String             @map("node_key") @db.VarChar(64)
  nodeName         String             @map("node_name") @db.VarChar(128)
  sortNo           Int                @map("sort_no")
  approverRoleCode String             @map("approver_role_code") @db.VarChar(32)
  createdAt        DateTime           @default(now()) @map("created_at")
  updatedAt        DateTime           @updatedAt @map("updated_at")
  template         WfApprovalTemplate @relation(fields: [templateId], references: [id])

  @@unique([templateId, nodeKey])
  @@unique([templateId, sortNo])
  @@index([templateId, sortNo])
  @@map("wf_approval_template_node")
}

model WfApprovalInstance {
  id                      BigInt              @id @default(autoincrement()) @db.UnsignedBigInt
  templateId              BigInt?             @map("template_id") @db.UnsignedBigInt
  businessType            String              @map("business_type") @db.VarChar(64)
  businessId              String              @map("business_id") @db.VarChar(64)
  title                   String              @db.VarChar(255)
  status                  String              @default("PENDING") @db.VarChar(20)
  applicantUserId         BigInt              @map("applicant_user_id") @db.UnsignedBigInt
  applicantRoleCode       String?             @map("applicant_role_code") @db.VarChar(32)
  currentNodeKey          String?             @map("current_node_key") @db.VarChar(64)
  currentNodeName         String?             @map("current_node_name") @db.VarChar(128)
  currentNodeSort         Int?                @map("current_node_sort")
  currentApproverRoleCode String?             @map("current_approver_role_code") @db.VarChar(32)
  currentApproverUserId   BigInt?             @map("current_approver_user_id") @db.UnsignedBigInt
  latestComment           String?             @map("latest_comment") @db.VarChar(500)
  formData                Json?               @map("form_data")
  finishedAt              DateTime?           @map("finished_at")
  withdrawnAt             DateTime?           @map("withdrawn_at")
  createdAt               DateTime            @default(now()) @map("created_at")
  updatedAt               DateTime            @updatedAt @map("updated_at")
  applicant               SysUser             @relation("ApprovalApplicant", fields: [applicantUserId], references: [id])
  currentApprover         SysUser?            @relation("ApprovalCurrentAssignee", fields: [currentApproverUserId], references: [id])
  template                WfApprovalTemplate? @relation(fields: [templateId], references: [id])
  logs                    WfApprovalNodeLog[]
  demoForm                DemoApprovalForm?
  memberRegularization    MemberRegularization?
  promotionApplication    PromApplication?
  competitionTeam         CompTeam?
  achievement             AchvAchievement?
  deviceRepair            AssetDeviceRepair?
  consumableRequest       InvConsumableRequest?
  fundApplication         FundApplication?

  @@index([businessType, businessId])
  @@index([status, currentApproverRoleCode])
  @@index([status, currentApproverUserId])
  @@index([applicantUserId, status])
  @@map("wf_approval_instance")
}

model WfApprovalNodeLog {
  id           BigInt              @id @default(autoincrement()) @db.UnsignedBigInt
  instanceId   BigInt              @map("instance_id") @db.UnsignedBigInt
  nodeKey      String?             @map("node_key") @db.VarChar(64)
  nodeName     String?             @map("node_name") @db.VarChar(128)
  actionType   String              @map("action_type") @db.VarChar(32)
  actorUserId  BigInt              @map("actor_user_id") @db.UnsignedBigInt
  actorRoleCode String?            @map("actor_role_code") @db.VarChar(32)
  targetUserId BigInt?             @map("target_user_id") @db.UnsignedBigInt
  comment      String?             @db.VarChar(500)
  extraData    Json?               @map("extra_data")
  createdAt    DateTime            @default(now()) @map("created_at")
  actor        SysUser             @relation("ApprovalActor", fields: [actorUserId], references: [id])
  instance     WfApprovalInstance  @relation(fields: [instanceId], references: [id])
  targetUser   SysUser?            @relation("ApprovalTransferTarget", fields: [targetUserId], references: [id])

  @@index([instanceId, createdAt])
  @@index([actorUserId, createdAt])
  @@map("wf_approval_node_log")
}

model DemoApprovalForm {
  id                 BigInt              @id @default(autoincrement()) @db.UnsignedBigInt
  title              String              @db.VarChar(128)
  reason             String              @db.VarChar(500)
  statusCode         String              @default("DRAFT") @map("status_code") @db.VarChar(20)
  applicantUserId    BigInt              @map("applicant_user_id") @db.UnsignedBigInt
  approvalInstanceId BigInt?             @unique @map("approval_instance_id") @db.UnsignedBigInt
  createdAt          DateTime            @default(now()) @map("created_at")
  updatedAt          DateTime            @updatedAt @map("updated_at")
  applicant          SysUser             @relation("DemoApprovalApplicant", fields: [applicantUserId], references: [id])
  approvalInstance   WfApprovalInstance? @relation(fields: [approvalInstanceId], references: [id])

  @@index([applicantUserId, statusCode])
  @@map("demo_approval_form")
}

model CompCompetition {
  id                    BigInt             @id @default(autoincrement()) @db.UnsignedBigInt
  competitionCode       String             @unique @map("competition_code") @db.VarChar(64)
  name                  String             @db.VarChar(255)
  organizer             String             @db.VarChar(255)
  competitionLevel      String             @map("competition_level") @db.VarChar(32)
  competitionCategory   String             @map("competition_category") @db.VarChar(64)
  statusCode            String             @default("OPEN") @map("status_code") @db.VarChar(20)
  registrationStartDate DateTime?          @map("registration_start_date") @db.Date
  registrationEndDate   DateTime?          @map("registration_end_date") @db.Date
  eventStartDate        DateTime?          @map("event_start_date") @db.Date
  eventEndDate          DateTime?          @map("event_end_date") @db.Date
  description           String?            @db.VarChar(1000)
  createdAt             DateTime           @default(now()) @map("created_at")
  updatedAt             DateTime           @updatedAt @map("updated_at")
  createdBy             BigInt?            @map("created_by") @db.UnsignedBigInt
  isDeleted             Boolean            @default(false) @map("is_deleted")
  teams                 CompTeam[]
  achievements          AchvAchievement[]  @relation("AchievementSourceCompetition")

  @@index([statusCode, competitionLevel])
  @@map("comp_competition")
}

model CompTeam {
  id                 BigInt              @id @default(autoincrement()) @db.UnsignedBigInt
  competitionId      BigInt              @map("competition_id") @db.UnsignedBigInt
  teamName           String              @map("team_name") @db.VarChar(128)
  teamLeaderUserId   BigInt              @map("team_leader_user_id") @db.UnsignedBigInt
  advisorUserId      BigInt?             @map("advisor_user_id") @db.UnsignedBigInt
  memberUserIds      String              @map("member_user_ids") @db.VarChar(500)
  memberNames        String              @map("member_names") @db.VarChar(500)
  projectId          String?             @map("project_id") @db.VarChar(64)
  projectName        String?             @map("project_name") @db.VarChar(128)
  applicationReason  String?             @map("application_reason") @db.VarChar(1000)
  statusCode         String              @default("DRAFT") @map("status_code") @db.VarChar(20)
  latestResult       String?             @map("latest_result") @db.VarChar(500)
  approvalInstanceId BigInt?             @unique @map("approval_instance_id") @db.UnsignedBigInt
  submittedAt        DateTime?           @map("submitted_at")
  completedAt        DateTime?           @map("completed_at")
  createdAt          DateTime            @default(now()) @map("created_at")
  updatedAt          DateTime            @updatedAt @map("updated_at")
  createdBy          BigInt?             @map("created_by") @db.UnsignedBigInt
  isDeleted          Boolean             @default(false) @map("is_deleted")
  competition        CompCompetition     @relation(fields: [competitionId], references: [id])
  teamLeader         SysUser             @relation("CompetitionTeamLeader", fields: [teamLeaderUserId], references: [id])
  advisor            SysUser?            @relation("CompetitionAdvisor", fields: [advisorUserId], references: [id])
  approvalInstance   WfApprovalInstance? @relation(fields: [approvalInstanceId], references: [id])
  achievements       AchvAchievement[]   @relation("AchievementSourceTeam")

  @@index([competitionId, statusCode])
  @@index([teamLeaderUserId, statusCode])
  @@map("comp_team")
}

model AchvAchievement {
  id                  BigInt              @id @default(autoincrement()) @db.UnsignedBigInt
  achievementType     String              @map("achievement_type") @db.VarChar(32)
  title               String              @db.VarChar(255)
  levelCode           String?             @map("level_code") @db.VarChar(32)
  statusCode          String              @default("DRAFT") @map("status_code") @db.VarChar(20)
  recognizedGrade     String?             @map("recognized_grade") @db.VarChar(64)
  scoreMapping        Json?               @map("score_mapping")
  projectId           String?             @map("project_id") @db.VarChar(64)
  projectName         String?             @map("project_name") @db.VarChar(128)
  sourceCompetitionId BigInt?             @map("source_competition_id") @db.UnsignedBigInt
  sourceTeamId        BigInt?             @map("source_team_id") @db.UnsignedBigInt
  description         String?             @db.VarChar(2000)
  applicantUserId     BigInt              @map("applicant_user_id") @db.UnsignedBigInt
  latestResult        String?             @map("latest_result") @db.VarChar(500)
  approvalInstanceId  BigInt?             @unique @map("approval_instance_id") @db.UnsignedBigInt
  submittedAt         DateTime?           @map("submitted_at")
  recognizedAt        DateTime?           @map("recognized_at")
  createdAt           DateTime            @default(now()) @map("created_at")
  updatedAt           DateTime            @updatedAt @map("updated_at")
  createdBy           BigInt?             @map("created_by") @db.UnsignedBigInt
  isDeleted           Boolean             @default(false) @map("is_deleted")
  applicant           SysUser             @relation("AchievementApplicant", fields: [applicantUserId], references: [id])
  sourceCompetition   CompCompetition?    @relation("AchievementSourceCompetition", fields: [sourceCompetitionId], references: [id])
  sourceTeam          CompTeam?           @relation("AchievementSourceTeam", fields: [sourceTeamId], references: [id])
  approvalInstance    WfApprovalInstance? @relation(fields: [approvalInstanceId], references: [id])
  paper               AchvPaper?
  ipAsset             IpAsset?
  contributors        AchvContributor[]

  @@index([achievementType, statusCode])
  @@index([projectId])
  @@index([applicantUserId, statusCode])
  @@map("achv_achievement")
}

model AchvPaper {
  id                  BigInt          @id @default(autoincrement()) @db.UnsignedBigInt
  achievementId       BigInt          @unique @map("achievement_id") @db.UnsignedBigInt
  journalName         String?         @map("journal_name") @db.VarChar(255)
  publishDate         DateTime?       @map("publish_date") @db.Date
  doi                 String?         @db.VarChar(128)
  indexedBy           String?         @map("indexed_by") @db.VarChar(64)
  authorOrder         String?         @map("author_order") @db.VarChar(32)
  correspondingAuthor String?         @map("corresponding_author") @db.VarChar(128)
  createdAt           DateTime        @default(now()) @map("created_at")
  updatedAt           DateTime        @updatedAt @map("updated_at")
  achievement         AchvAchievement @relation(fields: [achievementId], references: [id])

  @@map("achv_paper")
}

model IpAsset {
  id             BigInt          @id @default(autoincrement()) @db.UnsignedBigInt
  achievementId  BigInt          @unique @map("achievement_id") @db.UnsignedBigInt
  assetType      String          @default("SOFTWARE_COPYRIGHT") @map("asset_type") @db.VarChar(32)
  certificateNo  String?         @map("certificate_no") @db.VarChar(128)
  registrationNo String?         @map("registration_no") @db.VarChar(128)
  authorizedDate DateTime?       @map("authorized_date") @db.Date
  ownerUnit      String?         @map("owner_unit") @db.VarChar(128)
  remarks        String?         @db.VarChar(1000)
  createdAt      DateTime        @default(now()) @map("created_at")
  updatedAt      DateTime        @updatedAt @map("updated_at")
  achievement    AchvAchievement @relation(fields: [achievementId], references: [id])

  @@map("ip_asset")
}

model AchvContributor {
  id                      BigInt          @id @default(autoincrement()) @db.UnsignedBigInt
  achievementId           BigInt          @map("achievement_id") @db.UnsignedBigInt
  userId                  BigInt?         @map("user_id") @db.UnsignedBigInt
  contributorName         String          @map("contributor_name") @db.VarChar(128)
  contributorRole         String          @map("contributor_role") @db.VarChar(32)
  contributionRank        Int             @map("contribution_rank")
  isInternal              Boolean         @default(true) @map("is_internal")
  contributionDescription String?         @map("contribution_description") @db.VarChar(500)
  createdAt               DateTime        @default(now()) @map("created_at")
  achievement             AchvAchievement @relation(fields: [achievementId], references: [id])
  user                    SysUser?        @relation("AchievementContributorUser", fields: [userId], references: [id])

  @@index([achievementId, contributionRank])
  @@index([userId])
  @@map("achv_contributor")
}

model PromApplication {
  id                    BigInt              @id @default(autoincrement()) @db.UnsignedBigInt
  applicationNo         String              @unique @map("application_no") @db.VarChar(64)
  memberProfileId       BigInt              @map("member_profile_id") @db.UnsignedBigInt
  applicantUserId       BigInt              @map("applicant_user_id") @db.UnsignedBigInt
  schemeId              BigInt?             @map("scheme_id") @db.UnsignedBigInt
  targetPositionCode    String              @map("target_position_code") @db.VarChar(32)
  targetRoleCode        String?             @map("target_role_code") @db.VarChar(32)
  statusCode            String              @default("IN_APPROVAL") @map("status_code") @db.VarChar(32)
  qualificationPassed   Boolean             @default(false) @map("qualification_passed")
  qualificationSnapshot Json?               @map("qualification_snapshot")
  teamEvaluation        String?             @map("team_evaluation") @db.VarChar(1000)
  departmentReview      String?             @map("department_review") @db.VarChar(1000)
  publicNoticeResult    String?             @map("public_notice_result") @db.VarChar(500)
  latestResult          String?             @map("latest_result") @db.VarChar(500)
  approvalInstanceId    BigInt?             @unique @map("approval_instance_id") @db.UnsignedBigInt
  submittedAt           DateTime?           @map("submitted_at")
  completedAt           DateTime?           @map("completed_at")
  createdAt             DateTime            @default(now()) @map("created_at")
  updatedAt             DateTime            @updatedAt @map("updated_at")
  applicant             SysUser             @relation("PromotionApplicant", fields: [applicantUserId], references: [id])
  memberProfile         MemberProfile       @relation(fields: [memberProfileId], references: [id])
  scheme                EvalScheme?         @relation(fields: [schemeId], references: [id])
  approvalInstance      WfApprovalInstance? @relation(fields: [approvalInstanceId], references: [id])
  appointment           PromAppointment?

  @@index([memberProfileId, statusCode])
  @@index([applicantUserId, statusCode])
  @@map("prom_application")
}

model PromAppointment {
  id                   BigInt          @id @default(autoincrement()) @db.UnsignedBigInt
  applicationId        BigInt          @unique @map("application_id") @db.UnsignedBigInt
  memberProfileId      BigInt          @map("member_profile_id") @db.UnsignedBigInt
  applicantUserId      BigInt          @map("applicant_user_id") @db.UnsignedBigInt
  targetPositionCode   String          @map("target_position_code") @db.VarChar(32)
  targetRoleCode       String?         @map("target_role_code") @db.VarChar(32)
  appointmentStatus    String          @default("NOTICE_PENDING") @map("appointment_status") @db.VarChar(32)
  publicNoticeStatus   String          @default("PENDING") @map("public_notice_status") @db.VarChar(32)
  publicNoticeStartDate DateTime?      @map("public_notice_start_date") @db.Date
  publicNoticeEndDate  DateTime?       @map("public_notice_end_date") @db.Date
  publicNoticeResult   String?         @map("public_notice_result") @db.VarChar(500)
  appointedAt          DateTime?       @map("appointed_at")
  latestResult         String?         @map("latest_result") @db.VarChar(500)
  createdAt            DateTime        @default(now()) @map("created_at")
  updatedAt            DateTime        @updatedAt @map("updated_at")
  applicant            SysUser         @relation("PromotionAppointmentApplicant", fields: [applicantUserId], references: [id])
  application          PromApplication @relation(fields: [applicationId], references: [id])
  memberProfile        MemberProfile   @relation(fields: [memberProfileId], references: [id])

  @@index([memberProfileId, appointmentStatus])
  @@map("prom_appointment")
}

model AssetDevice {
  id                 BigInt              @id @default(autoincrement()) @db.UnsignedBigInt
  deviceCode         String              @unique @map("device_code") @db.VarChar(64)
  deviceName         String              @map("device_name") @db.VarChar(128)
  categoryName       String              @map("category_name") @db.VarChar(64)
  model              String?             @db.VarChar(128)
  specification      String?             @db.VarChar(255)
  manufacturer       String?             @db.VarChar(128)
  serialNo           String?             @map("serial_no") @db.VarChar(128)
  assetTag           String?             @map("asset_tag") @db.VarChar(64)
  statusCode         String              @default("IDLE") @map("status_code") @db.VarChar(32)
  orgUnitId          BigInt?             @map("org_unit_id") @db.UnsignedBigInt
  responsibleUserId  BigInt?             @map("responsible_user_id") @db.UnsignedBigInt
  locationLabel      String?             @map("location_label") @db.VarChar(128)
  purchaseDate       DateTime?           @map("purchase_date") @db.Date
  warrantyUntil      DateTime?           @map("warranty_until") @db.Date
  purchaseAmount     Decimal?            @map("purchase_amount") @db.Decimal(12, 2)
  remarks            String?             @db.VarChar(1000)
  latestRepairId     BigInt?             @unique @map("latest_repair_id") @db.UnsignedBigInt
  statusChangedAt    DateTime?           @map("status_changed_at")
  statusLogs         Json?               @map("status_logs")
  createdAt          DateTime            @default(now()) @map("created_at")
  updatedAt          DateTime            @updatedAt @map("updated_at")
  createdBy          BigInt?             @map("created_by") @db.UnsignedBigInt
  isDeleted          Boolean             @default(false) @map("is_deleted")
  orgUnit            OrgUnit?            @relation(fields: [orgUnitId], references: [id])
  responsibleUser    SysUser?            @relation("DeviceResponsibleUser", fields: [responsibleUserId], references: [id])
  latestRepair       AssetDeviceRepair?  @relation("DeviceLatestRepair", fields: [latestRepairId], references: [id], onDelete: SetNull, onUpdate: Cascade)
  repairOrders       AssetDeviceRepair[] @relation("DeviceRepairDevice")

  @@index([statusCode, orgUnitId])
  @@index([responsibleUserId, statusCode])
  @@map("asset_device")
}

model AssetDeviceRepair {
  id                     BigInt              @id @default(autoincrement()) @db.UnsignedBigInt
  deviceId               BigInt              @map("device_id") @db.UnsignedBigInt
  repairNo               String              @unique @map("repair_no") @db.VarChar(64)
  applicantUserId        BigInt              @map("applicant_user_id") @db.UnsignedBigInt
  applicantRoleCode      String?             @map("applicant_role_code") @db.VarChar(32)
  handlerUserId          BigInt?             @map("handler_user_id") @db.UnsignedBigInt
  statusCode             String              @default("DRAFT") @map("status_code") @db.VarChar(32)
  severity               String              @db.VarChar(16)
  faultDescription       String              @map("fault_description") @db.VarChar(2000)
  handlerComment         String?             @map("handler_comment") @db.VarChar(1000)
  resolutionSummary      String?             @map("resolution_summary") @db.VarChar(1000)
  latestResult           String?             @map("latest_result") @db.VarChar(500)
  requestedAmount        Decimal?            @map("requested_amount") @db.Decimal(12, 2)
  costEstimate           Decimal?            @map("cost_estimate") @db.Decimal(12, 2)
  actualCost             Decimal?            @map("actual_cost") @db.Decimal(12, 2)
  fundLinkCode           String?             @map("fund_link_code") @db.VarChar(64)
  deviceStatusBeforeRepair String?           @map("device_status_before_repair") @db.VarChar(32)
  approvalInstanceId     BigInt?             @unique @map("approval_instance_id") @db.UnsignedBigInt
  attachments            Json?               @map("attachments")
  statusLogs             Json?               @map("status_logs")
  reportedAt             DateTime            @default(now()) @map("reported_at")
  approvedAt             DateTime?           @map("approved_at")
  acceptedAt             DateTime?           @map("accepted_at")
  resolvedAt             DateTime?           @map("resolved_at")
  confirmedAt            DateTime?           @map("confirmed_at")
  statusChangedAt        DateTime?           @map("status_changed_at")
  createdAt              DateTime            @default(now()) @map("created_at")
  updatedAt              DateTime            @updatedAt @map("updated_at")
  createdBy              BigInt?             @map("created_by") @db.UnsignedBigInt
  isDeleted              Boolean             @default(false) @map("is_deleted")
  device                 AssetDevice         @relation("DeviceRepairDevice", fields: [deviceId], references: [id])
  applicant              SysUser             @relation("DeviceRepairApplicant", fields: [applicantUserId], references: [id])
  handler                SysUser?            @relation("DeviceRepairHandler", fields: [handlerUserId], references: [id])
  approvalInstance       WfApprovalInstance? @relation(fields: [approvalInstanceId], references: [id])
  latestForDevice        AssetDevice?        @relation("DeviceLatestRepair")

  @@index([deviceId, statusCode])
  @@index([applicantUserId, statusCode])
  @@index([handlerUserId, statusCode])
  @@index([reportedAt])
  @@map("asset_device_repair")
}

model InvConsumable {
  id                       BigInt               @id @default(autoincrement()) @db.UnsignedBigInt
  consumableCode           String               @unique @map("consumable_code") @db.VarChar(64)
  consumableName           String               @map("consumable_name") @db.VarChar(128)
  categoryName             String               @map("category_name") @db.VarChar(64)
  specification            String?              @db.VarChar(255)
  unitName                 String               @map("unit_name") @db.VarChar(32)
  statusCode               String               @default("ACTIVE") @map("status_code") @db.VarChar(20)
  inventoryStatus          String               @default("NORMAL") @map("inventory_status") @db.VarChar(20)
  currentStock             Decimal              @default(0) @map("current_stock") @db.Decimal(12, 2)
  warningThreshold         Decimal              @default(0) @map("warning_threshold") @db.Decimal(12, 2)
  warningFlag              Boolean              @default(false) @map("warning_flag")
  orgUnitId                BigInt?              @map("org_unit_id") @db.UnsignedBigInt
  defaultLocation          String?              @map("default_location") @db.VarChar(128)
  replenishmentTriggeredAt DateTime?            @map("replenishment_triggered_at")
  lastTxnAt                DateTime?            @map("last_txn_at")
  createdAt                DateTime             @default(now()) @map("created_at")
  updatedAt                DateTime             @updatedAt @map("updated_at")
  createdBy                BigInt?              @map("created_by") @db.UnsignedBigInt
  isDeleted                Boolean              @default(false) @map("is_deleted")
  orgUnit                  OrgUnit?             @relation(fields: [orgUnitId], references: [id])
  creator                  SysUser?             @relation("ConsumableCreatedByUser", fields: [createdBy], references: [id])
  txns                     InvInventoryTxn[]
  requests                 InvConsumableRequest[]

  @@index([categoryName, inventoryStatus])
  @@index([orgUnitId, inventoryStatus])
  @@map("inv_consumable")
}

model InvConsumableRequest {
  id                 BigInt              @id @default(autoincrement()) @db.UnsignedBigInt
  requestNo          String              @unique @map("request_no") @db.VarChar(64)
  consumableId       BigInt              @map("consumable_id") @db.UnsignedBigInt
  applicantUserId    BigInt              @map("applicant_user_id") @db.UnsignedBigInt
  applicantRoleCode  String?             @map("applicant_role_code") @db.VarChar(32)
  quantity           Decimal             @db.Decimal(12, 2)
  purpose            String              @db.VarChar(500)
  projectId          String?             @map("project_id") @db.VarChar(64)
  projectName        String?             @map("project_name") @db.VarChar(128)
  statusCode         String              @default("DRAFT") @map("status_code") @db.VarChar(32)
  latestResult       String?             @map("latest_result") @db.VarChar(500)
  approvalInstanceId BigInt?             @unique @map("approval_instance_id") @db.UnsignedBigInt
  outboundTxnId      BigInt?             @unique @map("outbound_txn_id") @db.UnsignedBigInt
  statusLogs         Json?               @map("status_logs")
  requestedAt        DateTime            @default(now()) @map("requested_at")
  completedAt        DateTime?           @map("completed_at")
  createdAt          DateTime            @default(now()) @map("created_at")
  updatedAt          DateTime            @updatedAt @map("updated_at")
  consumable         InvConsumable       @relation(fields: [consumableId], references: [id])
  applicant          SysUser             @relation("ConsumableRequestApplicant", fields: [applicantUserId], references: [id])
  approvalInstance   WfApprovalInstance? @relation(fields: [approvalInstanceId], references: [id])
  inventoryTxns      InvInventoryTxn[]   @relation("ConsumableRequestTxn")
  outboundTxn        InvInventoryTxn?    @relation("ConsumableRequestOutboundTxn", fields: [outboundTxnId], references: [id])

  @@index([consumableId, statusCode])
  @@index([applicantUserId, statusCode])
  @@index([projectId])
  @@map("inv_consumable_request")
}

model InvInventoryTxn {
  id               BigInt               @id @default(autoincrement()) @db.UnsignedBigInt
  consumableId     BigInt               @map("consumable_id") @db.UnsignedBigInt
  requestId        BigInt?              @map("request_id") @db.UnsignedBigInt
  txnType          String               @map("txn_type") @db.VarChar(32)
  quantity         Decimal              @db.Decimal(12, 2)
  balanceAfter     Decimal              @map("balance_after") @db.Decimal(12, 2)
  projectId        String?              @map("project_id") @db.VarChar(64)
  projectName      String?              @map("project_name") @db.VarChar(128)
  operatorUserId   BigInt?              @map("operator_user_id") @db.UnsignedBigInt
  operatorRoleCode String?              @map("operator_role_code") @db.VarChar(32)
  remark           String?              @db.VarChar(500)
  extraData        Json?                @map("extra_data")
  txnAt            DateTime             @map("txn_at")
  createdAt        DateTime             @default(now()) @map("created_at")
  consumable       InvConsumable        @relation(fields: [consumableId], references: [id])
  request          InvConsumableRequest? @relation("ConsumableRequestTxn", fields: [requestId], references: [id])
  operator         SysUser?             @relation("InventoryTxnOperator", fields: [operatorUserId], references: [id])
  requestOutbound  InvConsumableRequest? @relation("ConsumableRequestOutboundTxn")

  @@index([consumableId, txnAt])
  @@index([requestId])
  @@index([operatorUserId, txnAt])
  @@map("inv_inventory_txn")
}

model FundAccount {
  id              BigInt              @id @default(autoincrement()) @db.UnsignedBigInt
  accountCode     String              @unique @map("account_code") @db.VarChar(64)
  accountName     String              @map("account_name") @db.VarChar(128)
  statusCode      String              @default("ACTIVE") @map("status_code") @db.VarChar(32)
  categoryName    String              @map("category_name") @db.VarChar(64)
  projectId       String?             @map("project_id") @db.VarChar(64)
  projectName     String?             @map("project_name") @db.VarChar(128)
  ownerOrgUnitId  BigInt?             @map("owner_org_unit_id") @db.UnsignedBigInt
  managerUserId   BigInt?             @map("manager_user_id") @db.UnsignedBigInt
  totalBudget     Decimal             @map("total_budget") @db.Decimal(12, 2)
  reservedAmount  Decimal             @default(0) @map("reserved_amount") @db.Decimal(12, 2)
  usedAmount      Decimal             @default(0) @map("used_amount") @db.Decimal(12, 2)
  paidAmount      Decimal             @default(0) @map("paid_amount") @db.Decimal(12, 2)
  remarks         String?             @db.VarChar(1000)
  lastExpenseAt   DateTime?           @map("last_expense_at")
  createdAt       DateTime            @default(now()) @map("created_at")
  updatedAt       DateTime            @updatedAt @map("updated_at")
  createdBy       BigInt?             @map("created_by") @db.UnsignedBigInt
  isDeleted       Boolean             @default(false) @map("is_deleted")
  ownerOrgUnit    OrgUnit?            @relation(fields: [ownerOrgUnitId], references: [id])
  manager         SysUser?            @relation("FundAccountManager", fields: [managerUserId], references: [id])
  applications    FundApplication[]

  @@index([statusCode, ownerOrgUnitId])
  @@index([projectId])
  @@index([managerUserId, statusCode])
  @@map("fund_account")
}

model FundApplication {
  id                  BigInt              @id @default(autoincrement()) @db.UnsignedBigInt
  applicationNo       String              @unique @map("application_no") @db.VarChar(64)
  accountId           BigInt              @map("account_id") @db.UnsignedBigInt
  applicantUserId     BigInt              @map("applicant_user_id") @db.UnsignedBigInt
  applicantRoleCode   String?             @map("applicant_role_code") @db.VarChar(32)
  applicationType     String              @map("application_type") @db.VarChar(32)
  expenseType         String              @map("expense_type") @db.VarChar(32)
  title               String              @db.VarChar(255)
  purpose             String              @db.VarChar(1000)
  amount              Decimal             @db.Decimal(12, 2)
  reimbursementAmount Decimal?            @map("reimbursement_amount") @db.Decimal(12, 2)
  payeeName           String?             @map("payee_name") @db.VarChar(128)
  projectId           String?             @map("project_id") @db.VarChar(64)
  projectName         String?             @map("project_name") @db.VarChar(128)
  relatedBusinessType String?             @map("related_business_type") @db.VarChar(64)
  relatedBusinessId   String?             @map("related_business_id") @db.VarChar(64)
  statusCode          String              @default("DRAFT") @map("status_code") @db.VarChar(32)
  paymentStatus       String              @default("UNPAID") @map("payment_status") @db.VarChar(32)
  paymentRemark       String?             @map("payment_remark") @db.VarChar(500)
  latestResult        String?             @map("latest_result") @db.VarChar(500)
  approvalInstanceId  BigInt?             @unique @map("approval_instance_id") @db.UnsignedBigInt
  attachments         Json?               @map("attachments")
  statusLogs          Json?               @map("status_logs")
  submittedAt         DateTime?           @map("submitted_at")
  completedAt         DateTime?           @map("completed_at")
  paidAt              DateTime?           @map("paid_at")
  createdAt           DateTime            @default(now()) @map("created_at")
  updatedAt           DateTime            @updatedAt @map("updated_at")
  createdBy           BigInt?             @map("created_by") @db.UnsignedBigInt
  account             FundAccount         @relation(fields: [accountId], references: [id])
  applicant           SysUser             @relation("FundApplicationApplicant", fields: [applicantUserId], references: [id])
  approvalInstance    WfApprovalInstance? @relation(fields: [approvalInstanceId], references: [id])

  @@index([accountId, statusCode])
  @@index([applicantUserId, statusCode])
  @@index([projectId])
  @@index([paymentStatus, statusCode])
  @@map("fund_application")
}
