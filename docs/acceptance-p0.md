# 一期验收清单

## 已完成

- 成员转正：已接入统一审批中心，补齐日期顺序保护与关键单测
- 成果录入：已具备成果录入/编辑/提审与成果认定审批链路
- 设备报修：已具备报修、审批、维修处理、结果确认、状态留痕
- 耗材申领：已具备申领、审批、自动出库、库存流水与低库存状态维护
- 经费申请：已具备预算校验、预占、审批通过转已用、支付确认与状态留痕
- 晋升申请：已具备资格校验、申请、审批、公示发布与岗位任命链路
- 审批中心：已支持待办、已办、退回、转交、补充说明、撤回、通知
- 测试：补齐关键服务测试，并新增最小 Playwright 冒烟测试
- 文档：README、部署、环境变量、初始化数据、技术债、验收清单已整理

## 部分完成

- 登录与首页 Playwright：已覆盖最小冒烟链路，尚未覆盖七条主流程的完整浏览器回归
- 成果认定规则：当前为可扩展 placeholder，尚未接入完整可配置评分规则
- 文件链路：已预留并具备接口形态，生产 MinIO 需按环境变量完成真实接入验证

## 未完成

- 项目立项：当前仅通过 `projectId/projectName` 进行关联占位，未形成独立模块、表结构和审批链路

## 风险点

- 部分历史中文文案在仓库中存在编码遗留，功能不受影响，但建议统一转为 UTF-8 文本后再做一轮文案校对
- `pnpm lint` 依赖 TypeScript ESLint project service，首次执行在部分机器上仍可能较慢
- Playwright 仅为最小冒烟，若要支撑发布前全面回归，仍需补充业务流用例与浏览器环境安装

## 后续建议

- P1 独立建设项目立项模块，并把当前各模块的 `projectId/projectName` 占位关联迁回真实项目主数据
- 将成果认定、公示天数、审批 SLA 等规则进一步转入系统配置或字典
- 继续补充审批拒绝/撤回后的跨模块联动回归测试
